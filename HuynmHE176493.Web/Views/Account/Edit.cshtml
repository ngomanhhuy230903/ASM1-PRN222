@model HuynmHE176493.Data.Models.SystemAccount

@{
    ViewData["Title"] = "Chỉnh sửa tài khoản";
}

<h2>Chỉnh sửa tài khoản</h2>

<form asp-action="Edit" method="post">
    <input type="hidden" asp-for="AccountId" />  <!-- ✅ Đảm bảo ID được gửi -->

    <div class="form-group">
        <label asp-for="AccountName">Tên tài khoản</label>
        <input asp-for="AccountName" class="form-control" required />
        <span asp-validation-for="AccountName" class="text-danger"></span> <!-- ✅ Hiển thị lỗi -->
    </div>

    <div class="form-group">
        <label asp-for="AccountEmail">Email</label>
        <input asp-for="AccountEmail" class="form-control" required />
        <span asp-validation-for="AccountEmail" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="AccountRole">Vai trò</label>
        <select asp-for="AccountRole" class="form-control">
            <option value="1">Staff</option>
            <option value="2">Lecturer</option>
        </select>
    </div>

    <div class="form-group">
        <label asp-for="IsActive">Trạng thái</label>
        <input type="checkbox" asp-for="IsActive" />
    </div>

    <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
    <a asp-action="Index" class="btn btn-secondary">Hủy</a>
</form>
